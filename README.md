
🚀 User Management CRUD Application (React & Tailwind CSS)

This project is a simple and modern User Management (CRUD) application built with React. It uses Local Storage for data persistence and is styled with Bootstrap to enhance UI/UX quality.


🌟 Key Features

Create: Add new users via a form.

Read: Display a list of existing users, with an informative message for an empty list.

Update: Select a user from the list and edit their details.

Delete: Permanently delete users with a confirmation prompt.

Form Validation: Mandatory and rule-based validation for Name, Email, and Age fields.


🛠 Technologies Used

The project is built with popular tools and libraries from the modern JavaScript ecosystem:

React 19: Core library for building the user interface.

Bootstrap 5: Powerful frontend toolkit for fast, responsive, and mobile-first design.

React Hot Toast: Elegant notifications for user actions (Add/Delete/Update).

uuid: Generates unique IDs for records.

vitest : Blazing fast unit test framework for testing components.

📋 Prerequisites

Node.js (v16 or higher)

npm (v7 or higher)



![crudenhanced](https://github.com/user-attachments/assets/d6b63365-5de2-4c48-b381-13473157adce)



# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react/README.md) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

